name: Automatix Test
systems:
  testsystem: docker-test
vars:
  myvar: huhu
pipeline:
  - python: print(globals())
  - python: print(locals())
  - local: sleep 20
  - a=local: uptime
  - remote@testsystem: whoami
  - python: print('{myvar}')
  - python: raise Exception('moin')
